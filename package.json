{
  "name": "xcstring-editor",
  "version": "1.0.0",
  "description": "Web-based editor for xcstrings files",
  "scripts": {
    "dev": "cd backend && mvn spring-boot:run",
    "test": "cd backend && mvn test",
    "build": "cd backend && mvn clean package -DskipTests"
  },
  "keywords": ["xcstrings", "editor", "localization", "ios", "apple"],
  "author": "",
  "license": "AGPL-3.0"
}
